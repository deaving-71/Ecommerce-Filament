import { Layout } from "@/layout";

export default function Collections() {
    return (
        <Layout title="Ecom">
            <main>
                <div>
                    <h1 className="font-bold">Collections</h1>
                </div>
            </main>
        </Layout>
    );
}
